# Assest Backend — Phase 1 MVP Dependencies

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.27

# Settings & environment
pydantic==2.9.0
pydantic-settings==2.5.0
python-dotenv==1.2.2

# Database
sqlalchemy[asyncio]==2.0.35
aiosqlite==0.20.0
asyncpg==0.29.0
supabase==2.13.0

# Vector database
qdrant-client==1.11.0

# LLM — Groq (OpenAI-compatible SDK)
groq==0.11.0

# Embeddings — local (no API key needed)
numpy>=1.26.4,<2.0
sentence-transformers==3.1.0

# Document parsing
# unstructured==0.15.0  # Heavy — install when needed for PDF parsing

# PDF & Image Processing (Optional — for attachment OCR/extraction)
# Install for full attachment processing:
#   pip install PyPDF2 pytesseract pillow aiohttp
# Requires system dependency: brew install tesseract (macOS) or apt-get install tesseract-ocr (Ubuntu)
# PyPDF2==4.0.0  # PDF text extraction
# pytesseract==0.3.10  # OCR wrapper for Tesseract
# pillow==10.2.0  # Image processing
# aiohttp==3.9.2  # Async HTTP downloads

# PII scrubbing
presidio-analyzer==2.2.362
presidio-anonymizer==2.2.362

# Chunking
llama-index-core==0.13.0

# Full-text search (TIER 1 — BM25)
rank-bm25==0.2.2

# Tokenizer
tiktoken==0.7.0

# HTTP client
httpx==0.27.0

# Event-driven sync infrastructure (Phase 3a)
redis==5.0.1
pydantic-core==2.20.1

# Observability & distributed tracing
opentelemetry-api==1.24.0
opentelemetry-sdk==1.24.0
opentelemetry-instrumentation-fastapi==0.45b0
opentelemetry-instrumentation-sqlalchemy==0.45b0
opentelemetry-exporter-prometheus==0.45b0
prometheus-client==0.20.0

# Rate limiting
slowapi==0.1.9

# Security
bcrypt==4.2.0
python-jose[cryptography]==3.4.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.27.0

# Graph database (Phase 3b)
neo4j==5.17.0  # Works with both Neo4j and Memgraph
networkx==3.2

# GitHub connector (Phase 3b)
PyGithub==2.1.1

# Advanced observability (Phase 3b)
opentelemetry-exporter-jaeger==0.45b0
opentelemetry-instrumentation-requests==0.45b0

# Utilities
click==8.1.0
slack-bolt==1.20.0
slack-sdk==3.31.0
aiohttp>=3.10,<4.0

# Connectors & lightweight parsing
notion-client==2.2.1
google-api-python-client==2.122.0
google-auth-oauthlib==1.2.0
beautifulsoup4==4.12.3
pypdf==5.1.0

# Free Search & CPU Reranking
duckduckgo-search==6.2.1
flashrank==0.2.8
